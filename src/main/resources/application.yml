spring:
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://localhost:3306/mysql?characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true&useSSL=false
        username: root
        password: root

    jpa:
        database: mysql
        show-sql: true
        properties:
            hibernate:
                format_sql: true
                use_sql_comments: true